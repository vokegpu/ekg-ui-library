text chunk-based for textbox:
1- chunk is opportunish, any inserted text will occup any chunk if is not limited
2- if chunk is full a new one is created
3- copypasting should be calculated to fit or generate news chunks if necessary
4- if textbox is unfocused, once time should re-index and fix incorrect text align --- this may affect perf but idc for now
5- the user-programmer will use the `ekg::text` class for handling texts --- but should be like an equals one
